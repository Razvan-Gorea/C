#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void sorted(int argc, char *argv[]);

int main(int argc, char *argv[]){
    sorted(argc, argv);
    return 0;
}

void sorted(int argc, char *argv[]){
//temp = a[i];
//a[i] = a[j];
//a[j] = temp;
}
